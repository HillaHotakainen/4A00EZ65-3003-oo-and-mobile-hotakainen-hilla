

import SwiftUI

struct ContentView: View {
    var body : some View {
        VStack {
          Text("moi")
          Text("hei")
          Button(action: {},
                 label:  { Text("Calculate BMI") } )
          }
    }
}
